rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Auth-only scaffold: Firebase Storage is not active in runtime.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
