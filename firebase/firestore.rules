rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Auth-only scaffold: Firestore is not active in runtime.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
